apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ClusterSecretStore is cluster-scoped and both ExternalSecrets below explicitly
# declare their target namespaces. Do not set a namespace transformer here.
resources:
  - 00-cluster-secret-store.yaml
  - 10-dockerhub-pull-secret.yaml
